
import { poolstrategyLogRel } from 'visual-pool/helpers/poolstrategy-log-rel';
import { module, test } from 'qunit';

module('Unit | Helper | poolstrategy log rel');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = poolstrategyLogRel([42]);
  assert.ok(result);
});

